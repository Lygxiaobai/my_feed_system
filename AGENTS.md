# AGENTS.md

> **目标**：交付高质量、安全、可维护的代码。
>
> **语言**：Agent 输出、项目文档、代码注释和 Git Commit 使用简体中文；标识符、外部 API、协议字段和社区约定保持原规范。

## 1. 规则与上下文

- 遵守系统和开发者规则，以及用户明确要求。
- `AGENTS.md` 负责项目流程、安全和编码规则。
- `.spec/` 负责业务行为、架构边界和功能契约。
- 两者发生冲突时必须停止并说明，不得自行选择。
- `AGENTS.local.md`（不入版本库）记录本机与服务器的环境事实、部署拓扑和历史踩坑；**开始任何构建、部署或环境相关排查前必须先读**，其中的环境约束优先于你的默认假设。
- 每次踩到新的环境陷阱，或发现 `AGENTS.local.md` 的记录已过时，必须在收尾时更新该文件。
- 开始修改前检查 `git status`，保留用户已有的未提交修改。
- 开始编码前读取 `.spec/project/spec.md`，再读取受影响的父级 Spec、叶子 Spec 和 `eval.md`。

## 2. 每次代码变更必须遵守

### karpathy-guidelines

- 编码前明确假设、目标和可验证的成功标准。
- 选择能解决问题的最小方案，不增加未要求的功能和抽象。
- 只修改与任务直接相关的代码，不顺手重构无关内容。
- 修改后执行成功标准对应的验证。

### codebase-design

- 识别受影响的 Module、Interface、Implementation、Seam 和 Adapter。
- 优先设计小 Interface、深 Implementation，隐藏复杂度并提升 Locality、Leverage 和 Testability。
- 优先通过 Interface 测试，不绕过 Seam 测试内部实现。
- 没有真实变化点时，不新增 Seam、Adapter 或抽象。
- 架构建议不得覆盖用户要求、`AGENTS.md` 或 `.spec/`。

## 3. 需求澄清与文档

- 需求或架构存在歧义时，先暂停编码。
- 需要正式澄清、ADR 或术语记录时，由用户显式调用 `grill-with-docs`。
- 普通小修复不创建额外文档，只记录必要的假设和验收标准。
- 用户可见行为变化必须同步更新对应的 `spec.md` 和 `eval.md`。
- 架构边界、入口文件或 Module 归属变化必须更新相关父级 Spec。

## 4. 安全与编码

- 禁止提交密钥、令牌、口令和敏感数据。
- 外部输入必须进行类型、格式、长度和边界校验。
- SQL 使用参数化查询；命令、路径、文件和序列化操作必须采取针对性防护。
- 禁止未经授权的 `git reset --hard`、`git checkout --`、强制推送和大范围删除。
- 新增或实际修改的文件使用 UTF-8 无 BOM，不得批量修改无关文件。
- 关键逻辑和非显而易见的决策添加中文注释，说明原因而非重复代码。

## 5. 验证与交付

- 按风险选择最小充分验证，不进行无差别全量检查。
- 逻辑变更补充或更新相关测试；跨模块变更执行集成、契约或 `eval.md` 验证。
- 前端用户流程变化执行构建、类型检查或端到端验证。
- 未执行的测试和检查必须明确说明。
- 交付时说明：完成内容、实现原因、验证方式、剩余风险和潜在技术债。

## 6. 外部资料

- 优先依据本地代码、`AGENTS.md`、`.spec/` 和项目文档。
- 只有涉及外部 API、框架版本、标准规范或时效性信息时才联网检索。
- 联网时优先使用官方文档和权威资料，并记录影响实现的关键结论来源。
